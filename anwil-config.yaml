---
api:
    host: ""
    port: 8010
    staticPath: ./static

db:
    host: postgres # assumes app is started in with a docker compose
    port: 6634
    username: anwil
    databaseName: postgres
    migrationsDir: ./migrations

privateKeyPath: ./.keys/ed25519
debug: yes
